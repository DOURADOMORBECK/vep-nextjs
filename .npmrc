# Ensure lockfile is respected
package-lock=true

# Use exact versions from package-lock.json
save-exact=true

# Disable package-lock.json updates during install
prefer-frozen-lockfile=true

# Legacy peer deps resolution (prevents warnings)
legacy-peer-deps=true